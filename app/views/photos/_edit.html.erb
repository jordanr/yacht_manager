<li id="edit_photo_<%= edit.id %>" style="text-align: left; float: left">
    <%= image_tag edit.public_filename(:thumb)  %>
    <label style="float: left"><%= link_to_remote "Destroy", :url=>listing_photo_path(edit.listing.id, edit.id), :method=>:delete, :confirm=>"Are you sure?", :href=>listing_photo_path(edit.listing.id, edit.id), :success=>"$('photo_list').removeChild($('edit_photo_#{edit.id}'))" %></label>
</li>
